
### 删除HttpClient 4.5.3
<dependency>
    <groupId>org.junit.jupiter</groupId>
    <artifactId>junit-jupiter</artifactId>
    <version>5.9.2</version>
    <scope>compile</scope>
</dependency>

### 后端回退到这里：feat: 修复依赖冲突问题
// 回滚
git reset --hard e8322b3
// 强制推送
git push --force origin release_backend_lettergen_dev

### 前端回退到这里：fix:成都分支开发的功能
// 回滚
git reset --hard 87be087126a7b35
// 强制推送
git push --force origin release_front_成都项目_UK改造和验证码登录改造
git push --force old release_front_成都项目_UK改造和验证码登录改造