
## 账号
巴中值班值守测试环境：http://10.15.136.98:9000/。
admin  
zhishen@2018~!@

mayu zhishen@2018~!@
enyang zhishen@2018~!@


## 代码库
https://ezone.cdcom.tech/
账号 zbzs_liugy
 Zbzs_123

## 新GitLab代码库
地址：
账号：root
密码：bzzb@2026~!@

git remote add origin http://10.1.248.14/zbzs/dyh-lbzbzllzxt-front.git
git push -u origin release_front_巴中项目_dev
git fetch old
for branch in $(git branch -r | grep 'old/' | grep -v HEAD | sed 's|.*old/||'); do
  echo "推送分支: $branch"
  git push origin "old/${branch}:refs/heads/${branch}"
done
git push origin --tags

### 拉取代码
// 如果自己之前对git配置了代理，需要先取消
// 必须先移除git的https http两个代理
// 才能使用数创独立的vpn取拉取代码
git config --global --unset http.proxy
git config --global --unset https.proxy 


// vpn加速访问github
// 只对特定域名github设置代理
git config --global http.https://github.com socks5://127.0.0.1:7897


### 需求实现，逻辑备注
1. 值班室排班需求 ==> 仅修改item_text，item_value维持原始数值（兼容历史数据）
2. 首页左下角未获取到县区数据 ==> mapper查询中硬编码（depart_name = '县（区）总值班室'），所以必须确定组织机构中有这个 '县（区）总值班室' 部门。
3. 首报操作中，选择主送对象的人员树 ==> 用了字典枚举实现REPORT_LEADER_DEPT，后端提供新接口。

#### 注意点【发版后需要在界面上操作】
1. DUTYUSERTYPE，DUTYUSERTYPEVALID，仅修改item_text：带班领导改为驻守领导，二级带班领导改为二级值班领导。
2. REPORT_LEADER_DEPT，增加‘首报主送部门’枚举，两项：市委领导，市政府领导
3. 确认部门管理中，组织机构中存在 "市委领导，市政府领导" 这两个部门，及下面需要挂载有人员。
    ```
    测试环境市委领导人员: swld001
    市政府领导人员: szfld001
    ```

## 待做需求
1. 值班室排班改动
    ```
    // 模版表导出，支持时间（模版内部默认留5行，即最多5个角色）
    值班人员操作逻辑: 会先点击'导出原始数据'导出一份空的数据，填充后从而进行后续的导入（称为数据导入模版）
    
    // 针对市委市政府总值班室，需要增加一个功能：报省表导出
    支持导出文件名称填写
    支持部门选择、时间选择、
    角色选择 (驻守主要领导、带班领导、县级值班领导、值班领导、值班员)
    角色：主要看是否有 驻守主要领导 角色，有则需要改导出word中的文案）

    // 注意点
    导出word中的角色，需要与原始数据一一匹配（逻辑上做匹配）
    导出word中的值班电话，从组织机构中读取。
    ```

2. 首页上方4卡片数据统计均调整为以信息中的"事发时间"（事件中应该也记录了信息的事发时间）进行统计；
    ```
    首页4卡片的数据均来自接口"/statistics/attention"，展示字段值与接口响应字段对应如下：
    接报信息数量: todayInfoCount
    上报事件数量: submitEventCount
    跟踪事件数量: trackEventCount
    完结事件数量: endEventCount
    ```
3. 首页中之前的事件类型饼图统计，改为信息类型饼图统计，统计的数据来源改为"信息接报"表。
    ```
    首页事件类型饼图统计的接口是"/statistics/eventType",
    首页左下角县区柱状图接口是"/statistics/orgCode"
    ```

4. 在值班管理菜单下新增`政策文件`子菜单新开页面，菜单页面仅对权限为'市委市政府总值班室'的用户开放，页面中支持文件上传，上传后列表中展示文件名称、上传时间、操作，操作支持删除、下载。在首页，上方某位置增加政策文件按钮，所有用户都有权限点击，弹出窗口，展示最近一周内文件列表(文件名称)，并支持点击文件名称进行预览，和下载操作。文件支持excel、word、pdf格式。

#### 菜单显示逻辑
1. 但是为啥我用账号test002登陆上去，没有看到政策文件的左侧菜单呢？难道不是登陆的时候就调用一次接口/deptDutyTree吗？
    ```
    // 登录时不会靠 deptDutyTree 决定是否显示「政策文件」菜单！！
    // 登录后菜单加载流程：
    按角色返回菜单树：/sys/permission/getUserPermissionByToken ==> applyPolicyDocumentMenuFilter(前端2次过滤) => 渲染左侧菜单
    角色表：`sys_role_permission`

    /report/eventInfo/deptDutyTree：部门树，用于排班/值班等业务页，不是菜单权限
    /duty/leaderRoster/deptDutyTree：领导排班部门树，同样与左侧菜单无关
    ```


