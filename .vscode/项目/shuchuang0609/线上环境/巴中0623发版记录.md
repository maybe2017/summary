
### 【遗留】字典状态问题
字典key：dict_item_status ，控制是否使用字典项状态启用与禁用？

问题：那么与现在mapper里面的查询逻辑是冲突的，mapper默认查询所有启动的字典项

// 但是线上环境在库root1中查询，目前状态为'禁用'的字典项只有两个
// 一个就是 dict_item_status 
// 另一个是 三级值班人员
// 所以线上先不管
select * from sys_dict_item s where s.status = 0