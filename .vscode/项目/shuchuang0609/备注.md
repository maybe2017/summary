
##
巴中值班值守测试环境：http://10.15.136.98:9000/。  账号
admin  
zhishen@2018~!@

## 代码库
https://ezone.cdcom.tech/
账号 zbzs_liugy
 Zbzs_123

### 拉取代码
// 如果自己之前对git配置了代理，需要先取消
// 必须先移除git的https http两个代理
// 才能使用数创独立的vpn取拉取代码
git config --global --unset http.proxy
git config --global --unset https.proxy 


// vpn加速访问github
// 只对特定域名github设置代理
git config --global http.https://github.com socks5://127.0.0.1:7897

### 前端编译
nvm install 20.19.0
nvm use 20.19.0
删除 node_modules 文件夹和 yarn.lock 文件，然后重新执行 yarn install


## 
# 角色设定
你是一位资深的全栈架构师。这个项目的后端是基于 **JeecgBoot 框架**（org.jeecgframework.boot）进行二次开发的。
# 背景
shuchuang文件夹下，dyh-lbzbzllzxt-api是后端代码，dyh-lbzbzllzxt-front是前端代码。
# 注意点
你不要直接操作数据库，如果有必要时，需要操作的时候，需要经过我的许可。
我需要你接下来，分析并理解项目，帮我完成一些需求。

## 注意点
如果需求不明确，你尝试理解后仍不能完全确定，就先不动对应需求的代码，先向我确认。
## 需求1
值班室排班模块中，移除【节假日排班导出】按钮；移除“未完成下月排班计划单位”的统计；(这两个点应该只是改前端就可以)
## 需求2
统一所有单位的值班带班人员类型为：驻守主要领导、带班领导、县级值班领导、值班领导、值班员。

你帮忙看看值班带班人员类型是不是用到了字典枚举DUTYUSERTYPE呢？除了修改前端代码外，我是不是需要手动去修改字典配置？

## 需求3
排查一下，首页左下角未获取到县区数据，是什么原因呢？接口代码有问题吗

如果数据库数据中的endtime为null，使用createtime的话，会影响其他接口吗，如果只是影响该接口，你就可以改。

## 需求4
在跟踪事件、上报事件中进行首报操作时，“选择主送对象”时无法同步市委领导和市政府领导组织机构及人员，




##
1. 新需求A：首页统计相关数据均调整为以信息的"事发时间"进行统计；且统计的数据来源板块改为“信息接报”（现在是跟踪事件+上报事件，且必须为处置结束状态的数据。）

首页4卡片的数据均来自接口"/statistics/attention"，分别为接报信息数量、上报事件数量、跟踪事件数量、完结事件数量，分别使用响应结果中的todayInfoCount、submitEventCount、trackEventCount、endEventCount字段值来展示。

首页事件类型饼图统计的接口是"/statistics/eventType",

首页左下角县区柱状图接口是"/statistics/orgCode"


2. 对于之前的需求：在值班室排班，带班领导改为驻守领导，二级带班领导改为二级值班领导。数据库保存的是字典值，我是否可以仅修改字典标签来实现这个需求，这样历史数据也不会有迁移的问题？你帮我看下。
之前的DUTYUSERTYPE枚举值如下：   
('驻守主要领导', '驻守主要领导')
('带班领导', '带班领导')
('一级带班领导', '一级带班领导')
('二级带班领导', '二级带班领导')
('三级值班人员', '三级值班')
('值班员', '值班员')
DUTYUSERTYPEVALID枚举值如下：
('带班领导', '带班领导')
('一级带班领导', '一级带班领导')
('二级带班领导', '二级带班领导')   

我现在想把枚举item_text改回之前，仅改item_value来实现需求。


#### 注意点
3. 【值班室排班，带班领导改为驻守领导】但县级/领导排班模块如果先不改，维持现状。
    ```
    值班室排班页面：不受影响，可正常排班、导入、导出
    县级排班页面：新建/导入若填「驻守领导」，可能校验失败（仍要求「带班领导」）；已有 110 条旧数据显示可能不一致
    领导排班页面：若仍用旧类型名，与字典不完全一致，但一般仍可操作
    结论：维持现状不影响值班室排班；只影响县级/领导排班模块自身的命名一致性。 后续若要统一，再改 CountyRosterServiceImpl 等即可
    ```


对于之前的需求4，有新的需要，首报主送对象无法加载市委领导及市政府领导，市委领导和市政府领导在线上真实环境中，是两个真实的部门名称，我现在连的本地数据库里面的数据和线上环境不一致，所以orgcode不一致，那么是不是无法采用在代码里面硬编码的方式呢，你帮我看下在新建一个部门的时候，它的机构编码是不是动态生成的，而不是手动指定的。 如果不能使用硬编码的方式，需要怎么改呢，现在需要首报主送对象和抄送对象，都加载市委领导和市政府领导两个部门下的数据