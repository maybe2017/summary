## 部署

### 服务器信息
10.1.248.12
用户名：zlzx
密码：Zlzx@2026

// mysql
root
123456

// 临时切换到root
sudo su -

1. 上传tar包到/home/zlzx/daping下
2. cd /home/zlzx/daping & tar -xzf daping-deploy-20260717-101140.tar.gz && cd daping-deploy-20260717-101140
3. cp .env.docker.example .env.docker

    ```
    // 有配置需要调整的话
    vim .env.docker
    // 停止删除之前的容器并启动
    sudo docker stop daping-backend daping-frontend 2>/dev/null
    sudo docker rm daping-backend daping-frontend 2>/dev/null
    DOCKER_BUILDKIT=0 COMPOSE_DOCKER_CLI_BUILD=0 sudo docker compose -f docker-compose.external-db.yml   --env-file .env.docker up -d --build
    DOCKER_BUILDKIT=0 COMPOSE_DOCKER_CLI_BUILD=0 sudo docker compose -f docker-compose.external-db.yml   --env-file .env.docker up -d --build
    ``` 
4. 更新包【最新最快捷】
    ```
    以后每次发布（两条命令）本地先打包：
    ./deploy/pack.sh
    scp dist/daping-deploy-*.tar.gz zlzx@apec:~/daping/

    服务器执行: ~/daping/deploy.sh

    脚本会自动：
    1.选 ~/daping/ 下最新的 daping-deploy-*.tar.gz（不用改包名）
    2.解压到对应目录
    3.docker stop/rm daping-backend daping-frontend
    4.跑 patch-dockerfiles-mirror.sh
    5.docker compose up -d --build
    6.写 ~/daping/CURRENT 和 ~/daping/current 软链
    
    // 指定某个版本包启动：
    ~/daping/deploy.sh daping-deploy-20260717-001044.tar.gz
    ```    

## 重要操作记录

### ubantu设置DNS
1. 临时修改【会被还原】
   ```
   sudo vi /etc/resolv.conf
   sudo resolvectl flush-caches
   ```
2. 永久修改【20260720修改DNS=10.190.0.201】
   ```
   // #DNS= 这一行，取消注释（删除 #），并填入你的DNS服务器地址，多个地址用空格分隔
   sudo nano /etc/systemd/resolved.conf

   // 重启
   sudo systemctl restart systemd-resolved     
   ```